## 1. 实现
- [x] 1.1 在 `get_advanced_sysctl_params()` 添加 TCP 慢启动优化参数
- [x] 1.2 添加内存自动调优参数
- [x] 1.3 添加端口范围和 TIME_WAIT 优化参数
- [x] 1.4 添加 SYN 队列优化参数
- [x] 1.5 更新优化方案显示（show_optimization_plan）

## 2. 验证
- [x] 2.1 语法检查（bash -n 通过；已人工审查）
- [x] 2.2 在测试环境验证参数应用（Ubuntu 24.04 实机 --auto 写入并生效）
- [x] 2.3 确认旧内核自动跳过不支持参数（逐行应用时不支持项被计入跳过，不影响其余）

## 3. 文档
- [x] 3.1 更新 README（v2.3.0 已重写 README）
